/*
 * Integra Brand — Drop-in PostCSS config
 *
 * Copy to your project root as postcss.config.mjs
 * Required for Tailwind CSS v4
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
